{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch `gwh init`",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/gwh/",
            "cwd": "${workspaceFolder}",
            "args": [
                "init"
            ]
        }
    ]
}